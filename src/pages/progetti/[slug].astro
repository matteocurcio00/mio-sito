---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getCollection, render } from "astro:content";

export async function getStaticPaths() {
  const projects = await getCollection("projects", ({ data }) => !data.draft);

  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
---

<BaseLayout
  title={`Matteo Curcio | ${project.data.title}`}
  description={project.data.description}
>
  <article class="box project-detail">
    {project.data.cover && (
      <img
        src={project.data.cover}
        alt={project.data.title}
        class="detail-cover"
      />
    )}

    <div class="detail-header">
      <div class="meta-row">
        <span class={`status ${project.data.status === "Concluso" ? "status-done" : "status-ongoing"}`}>
          {project.data.status}
        </span>
        <span class="date">{project.data.pubDate.toLocaleDateString("it-IT")}</span>
      </div>

      <h1>{project.data.title}</h1>
      <p class="description">{project.data.description}</p>

      <div class="tags">
        {project.data.tags.map((tag) => (
          <span class="tag">{tag}</span>
        ))}
      </div>
    </div>

    <hr />

    <div class="project-content">
      <Content />
    </div>
  </article>
</BaseLayout>

<style>
  .project-detail {
    max-width: 920px;
    margin: 0 auto;
    overflow: hidden;
    transition:
      transform 0.25s ease,
      box-shadow 0.25s ease,
      border-color 0.25s ease;
  }

  .project-detail:hover {
    transform: translateY(-4px);
    border-color: rgba(199, 137, 37, 0.35);
    box-shadow: 0 18px 38px rgba(22, 32, 51, 0.13);
  }

  .detail-cover {
    width: 100%;
    max-height: 420px;
    object-fit: cover;
    display: block;
    border-radius: 16px;
    margin-bottom: 26px;
    transition:
      transform 0.35s ease,
      filter 0.3s ease;
  }

  .project-detail:hover .detail-cover {
    transform: scale(1.015);
    filter: saturate(1.03);
  }

  .detail-header h1 {
    margin-bottom: 12px;
  }

  .meta-row {
    display: flex;
    gap: 12px;
    align-items: center;
    flex-wrap: wrap;
    margin-bottom: 18px;
  }

  .status {
    display: inline-block;
    padding: 6px 10px;
    border-radius: 999px;
    font-size: 0.82rem;
    font-weight: 700;
    transition:
      background-color 0.2s ease,
      color 0.2s ease;
  }

  .status.done {
    background: #e8f7ee;
    color: #157347;
  }

  .status.ongoing {
    background: #eef4ff;
    color: #24324a;
  }

  .project-detail:hover .status.ongoing {
    background: #f3e0ba;
    color: #24324a;
  }

  .date {
    color: #6b7280;
  }

  .description {
    color: #5f6b7f;
    line-height: 1.75;
    font-size: 1.05rem;
  }

  .tags {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 16px;
  }

  .tag {
    display: inline-block;
    padding: 6px 10px;
    border-radius: 999px;
    background: #f3f6fb;
    color: #4b5563;
    font-size: 0.82rem;
    font-weight: 600;
    transition:
      background-color 0.2s ease,
      color 0.2s ease;
  }

  .project-detail:hover .tag {
    background: #eef4ff;
    color: #24324a;
  }

  .project-content {
    line-height: 1.8;
  }
</style>